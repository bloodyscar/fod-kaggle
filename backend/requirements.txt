fastapi>=0.111
uvicorn[standard]>=0.30
onnxruntime>=1.18
# If you have an NVIDIA GPU + CUDA installed, use this instead for much
# lower inference latency (uninstall onnxruntime first):
#   pip uninstall onnxruntime
#   pip install onnxruntime-gpu>=1.18
opencv-python-headless>=4.9
numpy>=1.26
websockets>=12.0

# Web admin dashboard + MySQL
SQLAlchemy>=2.0
PyMySQL>=1.1
pydantic-settings>=2.2
PyJWT>=2.8
# bcrypt is used directly, not through passlib: passlib 1.7.4 reads
# bcrypt.__about__, which bcrypt 4.1+ removed.
bcrypt>=4.1
httpx>=0.27
